server:
  applicationConnectors:
    - type: http
      port: 8080
  adminConnectors:
    - type: http
      port: 8081

database:
  driverClass: org.h2.Driver
  user: db-user
  #  password:
  url: jdbc:h2:mem:./test
  properties:
    charSet: UTF-8
  maxWaitForConnection: 1s
